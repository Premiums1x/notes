---
title: "双向队列"
date: 2026-08-03
section: "data-structure"
source: http://120.77.152.123:8088/posts/%e5%8f%8c%e5%90%91Queue
tags:
  - "data-structure"
  - "博客迁移"
---
# 双向队列

即头部、尾部都可以实现出队、入队的队列。

双向队列兼具栈与队列的逻辑，因此它可以实现这两者的所有应用场景，同时提供更高的自由度。

如果出入队列都在同一侧，则等效于*栈*，否则则是单向队列。

双向队列常用方法：

- push_first() 将元素添加至队首
- push_last() 将元素添加至队尾
- pop_first() 删除队首元素
- pop_last() 删除队尾元素
- peek_first() 访问队首元素
- peek_last() 访问队尾元素

## 实现：

- 链表：“双向链表”，因为同时需要能够满足两侧的出入队，将双向链表的头节点和尾节点视为双向队列的队首和队尾，同时实现在两端添加和删除节点的功能。
- 数组：使用环形数组来实现双向队列，队列的实现基础上，仅需增加“队首入队”和“队尾出队”的方法。*对于队首入队时*：注意左移索引时是否越界，同样通过通过取余操作实现 `front` 越过数组头部后回到尾部。
