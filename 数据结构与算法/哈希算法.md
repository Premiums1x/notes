---
title: "哈希算法"
date: 2026-08-07
section: "data-structure"
source: http://120.77.152.123:8088/posts/hashAlgo
tags:
  - "data-structure"
  - "博客迁移"
---
解决哈希冲突只能保证哈希表在遇到冲突时正常运行，但归根结底还是要想办法在根源上减少哈希冲突发生。

## 哈希算法：

如果哈希表的桶是均匀存储键值对的，那么效率会很高，反正如果集聚，则哈希表效率急剧下降。

键值对的分布情况由哈希函数决定。

哈希函数的计算步骤，先计算哈希值，再对数组长度取模： `index = hash(key) % capacity`

观察以上公式，当哈希表容量 capacity 固定时，哈希算法 hash() 决定了输出值，进而决定了键值对在哈希表中的分布情况。

所以，为了降低哈希冲突的发生概率，我们应当将注意力集中在哈希算法 hash() 的设计上。

### 哈希算法应具备的特点：

- 确定性：对于相同的输入，哈希算法应始终产生相同的输出。这样才能确保哈希表是可靠的。
- 效率高：计算哈希值的过程应该足够快。计算开销越小，哈希表的实用性越高。
- 均匀分布：哈希算法应使得键值对均匀分布在哈希表中。分布越均匀，哈希冲突的概率就越低。

### 几类简单哈希算法：

- 加法哈希：对输入的每个字符的 ASCII 码进行相加，将得到的总和作为哈希值。
- 乘法哈希：利用乘法的不相关性，每轮乘以一个常数，将各个字符的 ASCII 码累积到哈希值中。
- 异或哈希：将输入数据的每个元素通过异或操作累积到一个哈希值中。
- 旋转哈希：将每个字符的 ASCII 码累积到一个哈希值中，每次累积之前都会对哈希值进行旋转操作。

**使用大质数作为模数，可以最大化地保证哈希值的均匀分布**，因为质数与其他数不存在公约数，能减少因公约数而导致映射得到的哈希值“集聚”现象，选用质数且这个质数最好足够大，以尽可能消除周期性模式，提升哈希算法的稳健性。

#### 市面使用最广泛的哈希算法是SHA-2 系列中的 SHA-256 ，其未被攻破，在安全应用相关领域使用最多。

### 数据结构的哈希值：

编程语言会为不同数据类型的key提供内置的哈希算法，用于计算哈希表中的桶索引。以 Python 为例，我们可以调用 hash() 函数来计算各种数据类型的哈希值。

- 整数和布尔量的哈希值就是其本身。
- 浮点数和字符串的哈希值计算较为复杂，有兴趣的读者请自行学习。
- 元组的哈希值是对其中每一个元素进行哈希，然后将这些哈希值组合起来，得到单一的哈希值。
- 对象的哈希值基于其内存地址生成。通过重写对象的哈希方法，可实现基于内容生成哈希值。

### key的特殊情况

一般使用不可变对象作为哈希表的key，如不使用动态数组作为key，因为其内容一改变，哈希值就变，从而无法根据key查到原来的内容。

而对于自己写的类的对象（如链表节点），虽然其中成员变量可变，但对于这种对象的哈希值是基于内存地址生成。虽然成员变量变化，但整个对象的内存地址不改变，因此哈希值不变。
