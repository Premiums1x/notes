对于字典id这些常量（不会改变的值），且整个生命周期里**永远不会被修改**，也**不需要在页面模板上响应式渲染**。

1. 可以避免Vue的响应式性能浪费，写在 `data() { return { ... } }` 里的数据，Vue 2 都会对其进行递归遍历，加上 `getter / setter` 进行**响应式依赖收集**。
2. 避免组件每次实例化都重复分配内存
	data()在组件打开、重建时会执行一次，对象就会在内存中创建一次。
	写在外面作为 JS 模块顶层的常量，**在整个应用加载时只创建一次**，内存占用极小且永远固定。