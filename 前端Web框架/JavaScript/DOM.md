![[Pasted image 20260416142745.png]]
将HTML文件中的元素都封装成一个一个对象，用面向对象的思想和方式去操作每一个元素。
HTML文件就是一个HTML文档：最外层被封装成Docunment对象

JS对DOM的操作：
![[Pasted image 20260416143020.png]]


DOM操作：
![[Pasted image 20260416143405.png]]
伪数组：只有长度、索引，没有数组操作方法。

DOM操作：
1.获取到对应元素DOM对象
2.调用对象的属性、方法来对元素进行设置