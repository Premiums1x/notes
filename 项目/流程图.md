用户登录
    ↓
/api/auth/login  → 获取 accessToken, refreshToken, tenantId
    ↓
存入 authStore (Pinia) + localStorage 持久化
    ↓
/auth/me  → 获取用户信息（roles, permissions, tenants）
    ↓
存入 authStore.user
    ↓
跳转到 /dashboard
    ↓
┌─────────────────────────────────────┐
│  router.beforeEach 守卫触发          │
│                                     │
│  1. 检查登录态 → 已登录 ✓           │
│  2. 检查是否有用户信息 → 有 ✓        │
│  3. 检查动态路由是否已生成 → 否 ✗     │
└─────────────────────────────────────┘
    ↓
permissionStore.generateRoutes()
    ↓
/auth/menu  → 后端返回已按权限筛选的菜单数据
    ↓
transformMenuToRoutes() → 转换成路由配置
    ↓
router.addRoute() → 动态挂载
    ↓
permissionStore.menus = 菜单数据
    ↓
布局组件直接遍历 menus 渲染侧边栏
    ↓
用户看到有权限的菜单
