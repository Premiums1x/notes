web程序，完成用户列表渲染：
![[Pasted image 20260423110923.png]]

- 静态资源：
前端页面放在static文件夹内
txt文件放在resource根目录：
![[Pasted image 20260423123218.png]]

后端服务端结构：
最外层com.lancer包：
封装用户的类放在poji包下，
请求处理类放在Controller层下
![[Pasted image 20260423123406.png]]

Controller层的UserController类用以处理：请求用户信息的逻辑：
```
package com.lancer.Controller;  
  
import cn.hutool.core.io.IoUtil;  
import com.lancer.poji.User;  
import org.springframework.web.bind.annotation.RequestMapping;  
import org.springframework.web.bind.annotation.RestController;  
  
import java.io.InputStream;  
import java.nio.charset.StandardCharsets;  
import java.time.LocalDateTime;  
import java.time.format.DateTimeFormatter;  
import java.util.ArrayList;  
import java.util.List;  
  
//能自动转为JSON是因为用了这个注解，  
// 该注解底层又有一个@ResponseBody注解，直接将controller的返回值作为响应体返回给浏览器  
// ，若返回值为对象/集合-->JSON-->响应  
@RestController  
public class UserController {  
    @RequestMapping("/list")  
    public List<User> list() throws Exception{  
        //1.加载读取user.txt文件，获取用户数据  
        InputStream in =  this.getClass().getClassLoader().getResourceAsStream("user.txt");  
        //按行读取，每一行是一个对象，封装到users集合类中  
        ArrayList<String> lines = IoUtil.readLines( in, StandardCharsets.UTF_8,new ArrayList<>());  
  
        //2.解析用户数据，封装User对象，再存到List集合中  
        List<User> userList = lines.stream()  
                .map(  
                        line -> {  
                            //分割每部分，存到数组里  
                            String[] parts = line.split(",");  
                            Integer id = Integer.parseInt(parts[0]);  
                            String username = parts[1];  
                            String password = parts[2];  
                            String name = parts[3];  
                            Integer age = Integer.parseInt(parts[4]);  
                            LocalDateTime updateTime = LocalDateTime.parse(parts[5], DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));  
  
                            //根据解析的每个内容封装到User对象中  
                            return new User(id, username, password, name, age, updateTime);  
                        }  
                ).toList();  
  
    return userList;  
    //目标要JSON格式数据，此时若返回的是对象或集合，直接返回即可，服务器会自动将数据转为JSON格式  
    }  
  
  
}
```


前端利用vue钩子函数在mounted时就去调用一个search方法，而该search方法逻辑为：内部通过axios去请求"/list"路径的资源。

访问web服务器静态资源：`http://localhost:8080/user.html`
实际效果：
![[Pasted image 20260423144833.png]]




